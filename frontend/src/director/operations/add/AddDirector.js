import React, {Component} from 'react';

export default class AddDirector extends Component {
    render() {
        if (false) {
            // TODO if not authenticated return login page
        }
        if (false) {
            // TODO if not admin show unAuthorized page
        }
        return (
            <div>

            </div>
        );
    }
}